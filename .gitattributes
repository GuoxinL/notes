# 统一换行符：仓库内文本一律 LF。
# 背景：本机 git 常开 core.autocrlf=true，Windows 侧工具（Obsidian / VS Code）保存过的
# content/*.md 会变成 CRLF，被 scripts/build.mjs 读进 build/ 产物后（remark 会把 \r\n 原样
# 写进 code block 与 text 节点的 value），导致几篇无关文章出现大面积噪声 diff。
# 此处 eol=lf 覆盖 autocrlf，保证检出一律 LF。
* text=auto eol=lf

# 二进制：不做任何换行符转换
*.webp  binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.woff  binary
*.woff2 binary
*.pdf   binary
